# 헬스반장 — 요청 문구 모음

회사처럼 클로드를 길게 쓰기 어려운 곳에서, **필요한 줄만 복사해서 붙여넣으면**
바로 작업이 시작되도록 모아 둔 파일이다. 문구 안의 `<>` 부분만 바꿔 쓰면 된다.

> 이 파일은 오너가 쓰는 것이지, 앱 코드가 읽는 파일이 아니다. 마음대로 고쳐도
> 앱에는 아무 영향이 없다.

---

## 0. 매번 맨 앞에 붙이면 좋은 한 줄

새 대화를 시작할 때 이 줄을 먼저 붙이면, 상황 설명을 매번 다시 안 해도 된다.

```
Healthy 저장소 헬스반장 앱 작업이야. 브랜치는 claude/figma-design-application-qsacnt 쓰고, 끝나면 main 에 머지해서 배포까지 해줘. 한국어로 쉽게 설명해줘.
```

---

## 1. 화면 문구 고치기

```
운동 탭의 "<지금 문구>" 를 "<바꿀 문구>" 로 바꿔줘.
```

```
<화면 이름> 화면에서 "<문구>" 이 문장 빼줘. 필요 없어 보여.
```

```
인사말 문구 후보를 바꾸고 싶어. 지금 것들 보여주고, 내가 고르면 그걸로 바꿔줘.
```

---

## 2. 화면 정리 (너무 복잡해 보일 때)

```
<탭 이름> 탭이 너무 복잡해 보여. 뭘 빼면 좋을지 의견 먼저 주고, 내가 고르면 반영해줘.
```

```
<탭 이름> 탭 간격이 너무 좁아서 답답해. 넓혀줘.
```

```
<탭 이름> 탭에서 회색 안내문(설명글) 중에 없어도 되는 거 정리해줘.
```

---

## 3. 색·글씨·아이콘

```
앱 전체 <색 이름> 색을 <원하는 색> 으로 바꿔줘.
```

```
<화면 이름> 글씨가 너무 작아. 키워줘.
```

```
앱 아이콘 다시 만들어줘. <원하는 느낌: 예) 더 진하게 / 덤벨 크게 / 배경 밝게>
```

> 아이콘을 바꾸면 **웹은 바로 반영**되지만, 폰에 설치한 앱 아이콘은
> 새 APK 를 빌드해서 다시 설치해야 바뀐다.

---

## 4. 기능 추가·변경

```
<탭 이름> 탭에 <기능> 넣고 싶어. 어떻게 만들지 먼저 설명해주고, 괜찮으면 만들어줘.
```

```
<기능> 이 지금 어떻게 동작하는지 설명해줘. 코드 말고 사람 말로.
```

```
<화면> 에서 <동작> 하면 <결과> 가 나오게 해줘.
```

---

## 5. 안 될 때 / 이상할 때

```
<화면 이름> 에서 <증상> 이 생겨. 원인 찾아서 고쳐줘.
```

```
앱이 안 열려. 무슨 일인지 확인해줘.
```

```
방금 배포한 게 화면에 안 보여. 진짜 배포됐는지 확인해줘.
```

---

## 6. 배포

```
지금까지 작업한 거 main 에 머지해서 배포해줘. 배포 끝나면 진짜 반영됐는지 사이트 열어서 확인까지 해줘.
```

```
지금 라이브에 올라가 있는 게 최신 맞는지 확인해줘.
```

> 배포하면 **웹(PWA)** 과 **설치된 앱** 둘 다 자동으로 나간다.
> 웹 주소: https://kesjjang12-sudo.github.io/Healthy

---

## 6-1. 카카오 로그인

설정하다 막혔을 때:

```
카카오 로그인 설정하다 <어느 단계> 에서 막혔어. <화면에 뜬 오류 문구> 라고 떠.
```

다 하고 나서 확인:

```
카카오 로그인 설정 다 했어. 서버에 켜졌는지 확인해줘.
```

> 따라 하는 순서는 `docs/카카오로그인-따라하기.md` 에 있다.

---

## 7. 되돌리기

```
방금 한 거 되돌려줘. 그 전 상태로.
```

```
<날짜/커밋 설명> 로 되돌려줘.
```

---

## 8. 확인만 하고 싶을 때 (코드 안 건드림)

```
지금 <탭 이름> 탭이 어떻게 생겼는지 그림으로 보여줘. 코드는 아직 고치지 마.
```

```
<작업> 하면 뭐가 어떻게 바뀌는지 먼저 설명만 해줘. 반영은 내가 보고 결정할게.
```

---

## 알아두면 좋은 것

**바꾸면 안 되는 것** — 아래는 겉 이름과 달리 **바꾸면 앱이 깨진다.**
바꿔 달라고 하면 클로드가 이유를 설명하고 말릴 것이다.

| 값 | 왜 못 바꾸나 |
|---|---|
| `fitroutine` (slug·scheme) | 헬스장 기구에 붙인 QR 코드가 이 이름으로 앱을 연다 |
| `com.fitroutine.app` (패키지) | 바꾸면 안드로이드가 다른 앱으로 인식해 업데이트가 끊긴다 |

앱 **표시 이름**(헬스반장)은 위와 상관없이 자유롭게 바꿀 수 있다.

**반영되는 데 걸리는 시간**

| 대상 | 언제 보이나 |
|---|---|
| 웹(PWA) | 배포 후 1~2분, 새로고침하면 바로 |
| 설치된 앱 화면 | 앱 껐다 켜면 |
| 설치된 앱 **아이콘·이름** | 새 APK 빌드해서 재설치해야 |

**여러 세션이 같이 고친다** — 이 저장소는 여러 클로드 세션이 동시에 만진다.
그래서 배포 전에 항상 `main` 을 먼저 합치게 되어 있다. 배포해 달라고만 하면
클로드가 알아서 순서를 지킨다.
